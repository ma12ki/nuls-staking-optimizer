import React from "react";
import "./Header.css";

const Header = () => (
  <header className="Header">
    <h1 className="Header-title">NULS Staking Optimizer</h1>
  </header>
);

export default Header;
